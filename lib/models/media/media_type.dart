/// Enum representing different types of media that can be shared in conversations
enum MediaType {
  /// Photo/image files
  photo,
  
  /// Video files
  video,
  
  /// Document files (PDF, Word, etc.)
  document,
  
  /// Web links/URLs
  link,
  
  /// Voice messages/audio recordings
  voice,
}
