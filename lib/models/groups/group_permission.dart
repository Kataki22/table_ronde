/// Enum representing the permission levels in a group chat
enum GroupPermission {
  /// Administrator with full permissions
  admin,
  
  /// Moderator with limited management permissions
  moderator,
  
  /// Regular member with basic permissions
  member,
}
